{
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "routes": [
    {
      "src": "/photos/(?<id>[^/]+)",
      "dest": "/api/photos/[id]?id=$id"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
